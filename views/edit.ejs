<% layout("layouts/boilerplate") %>
    <form action="/campground/<%= campground._id %>?_method=patch" method="post">
        <div>
            <label for="title">Title: </label>
            <input type="text" name="title" id="title" placeholder="<%= campground.title %>">
        </div>
        <div>
            <label for="price">Price: </label>
            <input type="number" name="price" id="price", placeholder="<%= campground.price %>">
        </div>
        <div>
            <label for="description">Description: </label>
            <textarea name="description" id="description" cols="30" rows="10" placeholder="<%= campground.description %>"></textarea>
        </div>
        <div>
            <label for="location">Location: </label>
            <input type="text" name="location" id="location" placeholder="<%= campground.location %>">
        </div>
        <input type="submit" value="Edit Campground">
    </form>